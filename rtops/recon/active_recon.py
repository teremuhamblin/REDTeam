"""Module de reconnaissance active minimal (lab only)."""

def run(target: str):
    # Simule une collecte active (ne pas exécuter contre des cibles réelles)
    return {"target": target, "open_ports": [22, 80]}
